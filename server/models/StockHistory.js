class StockHistory {

    constructor(historyData) {
        this.body = historyData.body;
        //result.body.chart.result[0].indicators.quote[0].close[0];
    }
}

module.exports.StockHistory = StockHistory;